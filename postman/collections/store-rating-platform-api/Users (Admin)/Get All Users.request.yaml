$kind: http-request
method: GET
url: '{{baseUrl}}/users'
order: 1000
description: |-
  Retrieves a list of all users on the platform. Requires Admin authentication.
auth:
  type: bearer
  bearer:
    - key: token
      value: '{{token}}'
headers: []
